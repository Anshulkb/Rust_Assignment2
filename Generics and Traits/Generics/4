
// here we r passing 2 different types so T, S are used as arguments
struct Point<T,S> {
    x: T,
    y: S,
}

fn main() {
    // DON'T modify this code.
    let p = Point{x: 5, y : "hello".to_string()};

    println!("Success!");
}
